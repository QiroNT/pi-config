{
	"name": "pi-extension-gondolin",
	"private": true,
	"version": "0.82.1",
	"type": "module",
	"scripts": {
		"clean": "echo 'nothing to clean'",
		"build": "echo 'nothing to build'",
		"build:image": "gondolin build --config image.json --tag ghcr.io/catthehacker/ubuntu:custom-latest",
		"check": "node --experimental-strip-types --check index.ts"
	},
	"pi": {
		"extensions": [
			"./index.ts"
		]
	},
	"dependencies": {
		"@earendil-works/gondolin": "0.12.0"
	}
}
